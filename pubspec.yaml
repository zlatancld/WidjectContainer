name: widject_container
description: Dependency Injection package for Flutter.
version: 0.1.1
homepage: https://github.com/zlatancld/WidjectContainer
repository: https://github.com/zlatancld/WidjectContainer

environment:
  sdk: '>=2.19.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

# The following section is specific to Flutter.
flutter:
