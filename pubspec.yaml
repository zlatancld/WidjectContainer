name: widject_container
description: Dependency Injection Container package to structure your application in loosely coupled components. Nested scopes support to separate bindings by widget type. Embedded initialization system to easily initialize registered instances.
version: 0.1.7
homepage: https://github.com/zlatancld/WidjectContainer
repository: https://github.com/zlatancld/WidjectContainer

environment:
  sdk: '>=2.19.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

# The following section is specific to Flutter.
flutter:
